use mybatis;

create table t_user (
  id varchar(36),
  username varchar(500)
);

create table t_car (
  id varchar(36),
  name varchar(100),
  owner_id varchar(36)
);

create table t_book (
  id varchar(36),
  name varchar(100),
  owner_id varchar(36)
);

